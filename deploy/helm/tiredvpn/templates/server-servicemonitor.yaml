{{- if and .Values.server.enabled .Values.server.config.api.enabled .Values.server.metrics.serviceMonitor.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "tiredvpn.server.fullname" . }}
  namespace: {{ default .Release.Namespace .Values.server.metrics.serviceMonitor.namespace }}
  labels:
    {{- include "tiredvpn.server.labels" . | nindent 4 }}
    {{- with .Values.server.metrics.serviceMonitor.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  selector:
    matchLabels:
      {{- include "tiredvpn.server.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/role: api
  endpoints:
    - port: api
      path: /metrics
      interval: {{ .Values.server.metrics.serviceMonitor.interval }}
      {{- with .Values.server.metrics.serviceMonitor.scrapeTimeout }}
      scrapeTimeout: {{ . }}
      {{- end }}
      honorLabels: {{ .Values.server.metrics.serviceMonitor.honorLabels }}
      {{- with .Values.server.metrics.serviceMonitor.relabelings }}
      relabelings:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.server.metrics.serviceMonitor.metricRelabelings }}
      metricRelabelings:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
